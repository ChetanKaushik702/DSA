#include <stdio.h>

int main() {
    int age = 71;
    if (age >= 18 && age <= 100);
        printf("You are eligible to vote.");
    else
        printf("You aren\'t eligible to vote.");
    printf("Printing...");

    for (int i = 5 ; i != 3 ; ) {
        if (i % 2 == 0)
            i++;
        else
            i--;
        printf("%d", i);
    }
}

# include <stdio.h> 
# define scanf "%s Geeks For Geeks " 
main() 
{ 
    printf(scanf, scanf); 
    getchar(); 
    return 0; 
}

#include <iostream> 
using namespace std; 
int main() 
{ 
	for (5; 2; 2) 
		printf("Hello\n"); 
	return 0; 
} 


#include <stdio.h> 

int main() 
{ 
	static int i; 
	for (i++; ++i; i++) { 
		printf("%d ", i); 
		if (i == 6) 
			break; 
	} 
	return 0; 
} 

